#include <list>

// объявление класса атрибута
class Attribute;
// объявление класса отношений
class Relationship;

// класс сущности
class Entity
{
private:
public:
    Entity();
    // void addAttribute(Attribute);
    ~Entity();
};