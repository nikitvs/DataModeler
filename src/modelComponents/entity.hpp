#include <list>

// объявление класса сущности
class Attribute;

// класс отношений между сущностями
class Entity
{
private:

public:
    Entity();
    // void addAttribute(Attribute);
    ~Entity();
};